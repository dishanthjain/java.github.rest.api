# java.github.rest.api
Java code that uses GitHub’s REST API to add a collaborator to a repository by sending a PUT request with your GitHub personal access token, the repository owner, repository name, collaborator’s username, and optional permission level (pull, push, or admin).
